---
const { eyebrow, title, description, tone = 'default' } = Astro.props;
---

<section class={`page-hero tone-${tone}`}>
  <div>
    <p class="eyebrow">{eyebrow}</p>
    <h1>{title}</h1>
    <p class="hero-copy">{description}</p>
  </div>
  <slot />
</section>
