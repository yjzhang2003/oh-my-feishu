{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "installCommand": "npm ci --prefix showcase",
  "buildCommand": "npm run build --prefix showcase && rm -rf .vercel/output && mkdir -p .vercel && cp -R showcase/.vercel/output .vercel/output"
}
